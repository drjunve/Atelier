package com.edu.springboot.smtp;

import java.util.ArrayList;

import org.apache.ibatis.annotations.Mapper;


@Mapper
public interface _Imember {

	public int selectOne(InfoDTO infoDTO);
	
	public ArrayList<_MemberDTO> seletList(InfoDTO infoDTO);
	
	public int write(_MemberDTO memberDTO);
}
